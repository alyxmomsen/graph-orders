/******************************************************************************

                              Online C++ Compiler.
               Code, Compile, Run and Debug C++ program online.
Write your code in this editor and press "Run" button to compile and execute it.

*******************************************************************************/

#include <iostream>
#include <string>

// template <T>
void init (int capacity) ;

class Vertex ;

class Graph ;


const int MAX = 1000 ;



class Order {
    
    
    
}


class GraphVertex {
    GraphVertex* neighbours[MAX] ;
    Order* data ;
public:
    Vertex* addEdge(GraphVertex* vertex) {
        for (int i=0 ; i<MAX ; i++) {
            GraphVertex* value = neighbours[i] ;
            if(value==nullptr) {
                value = vertex ;
            }
        }
        
        return nullptr ;
    }
    GraphVertex(Order* order) {
        
        for (int i=0 ; i<MAX ; i++) {
            neighbours[i]=nullptr;
        }
    }
};

class Graph {
    
    Vertex* vertices[MAX] ;
    
    
    Vertex* add() {
        
        new GraphVertex();
        
        return 
    }
    
public:
    Graph () {
        
        for (int i=0 ; i<MAX ; i++) {
            
            this->vertices[i] = nullptr ;
        }
        
    }
};


int main()
{
    std::cout<<"end the programm";

    return 0;
}


void init (int capacity) {
    for (int i=0 ; i<capacity ; i++) {
        
        
    }
}

